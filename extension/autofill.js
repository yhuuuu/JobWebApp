/**
 * Universal job application auto-filler.
 * Works on LinkedIn Easy Apply, Workday, Greenhouse, Lever, iCIMS,
 * SmartRecruiters, Ashby, BambooHR, and generic HTML forms.
 *
 * NOT a standalone script — the fill logic runs inline via
 * chrome.scripting.executeScript({ func }) in popup.js so we can
 * pass profile data as an argument. This file is kept for reference
 * and potential future use as a content script.
 */
